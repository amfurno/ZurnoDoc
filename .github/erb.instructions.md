---
applyTo: '**/*.html.erb'
---
When adding a form with a cancel button, use a wrapper with class `block` to create space between the form fields and any buttons.
Try not to add inline styles. If you are trying to fix a layout issue, look for a way to solve it using Bulma's built-in classes. If you can't find a solution, ask for additional documentation from bulma to reference.